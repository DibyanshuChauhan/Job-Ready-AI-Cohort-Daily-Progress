# Backend API Base URL (default is the deployed Render URL, or http://localhost:3000/api/v1 for local)
VITE_API_BASE_URL=https://job-ready-ai-cohort-daily-progress-2.onrender.com/api/v1

# Backend Server URL for Google OAuth (default is the deployed Render URL, or http://localhost:3000 for local)
VITE_BACKEND_URL=https://job-ready-ai-cohort-daily-progress-2.onrender.com
